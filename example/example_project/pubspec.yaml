name: example_project
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ">=3.1.0 <4.0.0"
  flutter: ">=3.13.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.18.0

  trace:
    path: D:/workspace/Flutter/Packages/trace

dependency_overrides:
  meta: 1.9.1
  collection: 1.17.2

dev_dependencies:
  flutter_lints: ^2.0.3

flutter:
  uses-material-design: true
  generate: true

flutter_intl:
  enabled: true
